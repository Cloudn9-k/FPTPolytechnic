CREATE DATABASE ASM_SOF3062;
GO
USE ASM_SOF3062;
GO

-- 1. Bảng Tài khoản (Khách hàng & Admin) [cite: 34, 37]
IF OBJECT_ID('Accounts', 'U') IS NOT NULL DROP TABLE Accounts;
CREATE TABLE Accounts (
    username NVARCHAR(50) PRIMARY KEY,
    password NVARCHAR(100) NOT NULL,
    fullname NVARCHAR(100) NOT NULL,
    email NVARCHAR(100) NOT NULL,
    photo NVARCHAR(200),
    activated BIT DEFAULT 1, 
    admin BIT DEFAULT 0 -- 0: User, 1: Admin
);

-- 2. Bảng Loại hàng [cite: 30]
IF OBJECT_ID('Categories', 'U') IS NOT NULL DROP TABLE Categories;
CREATE TABLE Categories (
    id NVARCHAR(50) PRIMARY KEY,
    name NVARCHAR(100) NOT NULL
);

-- 3. Bảng Sản phẩm [cite: 30, 36]
IF OBJECT_ID('Products', 'U') IS NOT NULL DROP TABLE Products;
CREATE TABLE Products (
    id INT IDENTITY(1,1) PRIMARY KEY,
    name NVARCHAR(100) NOT NULL,
    image NVARCHAR(200),
    price FLOAT NOT NULL,
    create_date DATE DEFAULT GETDATE(),
    available BIT DEFAULT 1,
    category_id NVARCHAR(50) REFERENCES Categories(id)
);

-- 4. Bảng Đơn hàng [cite: 33, 38]
IF OBJECT_ID('Orders', 'U') IS NOT NULL DROP TABLE Orders;
CREATE TABLE Orders (
    id INT IDENTITY(1,1) PRIMARY KEY,
    username NVARCHAR(50) REFERENCES Accounts(username),
    create_date DATETIME DEFAULT GETDATE(),
    address NVARCHAR(200) NOT NULL
);

-- 5. Bảng Chi tiết đơn hàng
IF OBJECT_ID('OrderDetails', 'U') IS NOT NULL DROP TABLE OrderDetails;
CREATE TABLE OrderDetails (
    id INT IDENTITY(1,1) PRIMARY KEY,
    order_id INT REFERENCES Orders(id),
    product_id INT REFERENCES Products(id),
    price FLOAT,
    quantity INT
);

-- DATA MẪU DEMO
INSERT INTO Categories VALUES ('DH', N'Đồng hồ'), ('MT', N'Máy tính');
INSERT INTO Accounts VALUES ('admin', '123', N'Quản trị viên', 'admin@fpt.edu.vn', '', 1, 1);
INSERT INTO Accounts VALUES ('user', '123', N'Nguyễn Văn User', 'user@fpt.edu.vn', '', 1, 0);
INSERT INTO Products (name, price, category_id) VALUES (N'Rolex Fake', 1000, 'DH'), (N'Dell XPS', 2000, 'MT');